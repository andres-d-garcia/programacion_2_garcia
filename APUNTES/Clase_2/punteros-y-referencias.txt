Programacion II - Tema 1 - Punteros 

- Introduccion a los Punteros:

    Un puntero es una variable especial que almacena la dirección de memoria de esta variable

- Operadores de Punteros

    Operador & (direccion) ofrece la direccion, Operador * (indireccion) ofrece el valor

- Paso por Referencia

    void incrementar(int x){
        x = x + 1
    }

    int z = 2;
    incrementar(z);
    cout << z; // suelta 2 porque no hay un puntero en la funcion incrementar

    - Cada caracter en un string pesa 1 byte
    - Los punteros pesan 8 bytes

CRUD (CREATE, READ, UPDATE, DELETE)

editarLibro(Libro*L1, Libro*L2) en lugar de Libro L1, Libro L2

- Memoria Dinamica

- Punteros con Arreglos
- Punteros con estructuras 
- Punteros a funciones
- Deep clone vs Shallow clone
- Ejercicios practicos


// AVERIGUAR PORQUE LOS PUNTEROS PESAN 4 Y 8 BYTES DEPENDE DEL PROCESADOR
